export enum ItemMovement {
   add = 'add',
   delete = 'delete'
}

export enum Locations {
    refrigerator = 'Refrigerator',
    freezer = 'Freezer',
    pantry = 'Pantry'
}

export enum Vocab {
    expired = 'EXPIRED'
}