export enum ItemMovement {
   add = 'add',
   delete = 'delete'
}

export enum Locations {
    refrigerator = 'Regrigerator',
    freezer = 'Freezer',
    pantry = 'Pantry'
}

export enum Vocab {
    expired = 'EXPIRED'
}